import { render } from '@testing-library/react';
import App from './App';

test('App renders without errors', () => {
  render(<App />);
  // Verifica que la aplicación se renderice sin errores.
});
